"""Deterministic risk engine + ML fraud detection service.

Scope (see CLAUDE.md and this branch's task description): deterministic risk
signals, feature engineering, anomaly/supervised models, a provider
abstraction, a model registry, and a prediction API. This package produces
recommendations and evidence for human analysts — it never takes or implies
an autonomous adverse AML action.
"""

__version__ = "0.1.0"
