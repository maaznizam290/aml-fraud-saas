# Exact versions this branch was developed and tested against, for
# reproducibility. pyproject.toml carries the looser ranges consumers should
# actually depend on; this file is what `pip install -r requirements.txt`
# uses to recreate the same environment.
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.2.0
duckdb==1.5.5
fastapi==0.141.1
pydantic==2.13.5
uvicorn==0.52.4
pytest==9.1.1
httpx==0.28.1
